# File: monitoring/processor_execution_monitoring/job-config.env
# Configuration for Processor Execution Monitor Cloud Run Job

# GCP Configuration
PROJECT_ID="nba-props-platform"
REGION="us-west2"
JOB_NAME="processor-execution-monitor"

# Service Account
SERVICE_ACCOUNT="756957797294-compute@developer.gserviceaccount.com"

# Resource Configuration
CPU="1"
MEMORY="2Gi"
TIMEOUT="30m"
MAX_RETRIES="2"

# Default Arguments (can be overridden when executing)
DEFAULT_ARGS="--lookback-days=7"

# Scheduling (for Cloud Scheduler setup)
# During Season: Check hourly
SCHEDULE_SEASON="0 * * * *"
# Off-season: Check twice daily
SCHEDULE_OFFSEASON="0 8,17 * * *"

# Notification Webhooks (loaded from .env)
# SLACK_WEBHOOK_EXECUTION_ERROR (maps to SLACK_WEBHOOK_MONITORING_ERROR in .env)
# SLACK_WEBHOOK_EXECUTION_WARNING (maps to SLACK_WEBHOOK_MONITORING_WARNING in .env)
# SLACK_WEBHOOK_EXECUTION_CRITICAL (maps to SLACK_WEBHOOK_MONITORING_CRITICAL in .env)

# Email settings (loaded from .env)
# BREVO_SMTP_USERNAME
# BREVO_SMTP_PASSWORD
# BREVO_FROM_EMAIL
# EMAIL_ALERTS_TO
# EMAIL_CRITICAL_TO
################################################################################
# File: monitoring/processing_gap_detection/job-config.env
################################################################################
# Cloud Run Job Configuration for Processing Gap Monitor

# Job identification
JOB_NAME=processing-gap-monitor
REGION=us-west2
PROJECT_ID=nba-props-platform
SERVICE_ACCOUNT=756957797294-compute@developer.gserviceaccount.com

# Resource allocation
CPU=1
MEMORY=512Mi
MAX_RETRIES=1
TIMEOUT=600s

# Scheduling Configuration
# During Season: Hourly 10 AM - 6 PM PT (17:00 - 01:00 UTC)
# Offseason: Twice daily at 11 AM PT (18:00 UTC) and 5 PM PT (00:00 UTC)
SCHEDULE_SEASON="0 17-1 * 10-6 *"
SCHEDULE_OFFSEASON="0 18,0 * 7-9 *"
SCHEDULE_TIMEZONE="America/Los_Angeles"

# Default job arguments (can be overridden at execution time)
DEFAULT_ARGS="--lookback-days=1"

# Notification settings (loaded from environment at runtime)
# EMAIL_ALERTS_ENABLED=true
# EMAIL_ALERTS_TO=alerts@example.com
# SLACK_ALERTS_ENABLED=false
# SLACK_WEBHOOK_URL=https://hooks.slack.com/...

# FILE: backfill_jobs/scrapers/nbac_injury/job-config.env
# 
# Configuration for NBA.com Injury Reports PDF Download Job
# Downloads injury report PDFs using hourly interval strategy

# Job identification
JOB_NAME="nbac-injury-backfill"
JOB_SCRIPT="backfill_jobs/scrapers/nbac_injury/nbac_injury_scraper_backfill.py"
JOB_DESCRIPTION="NBA.com Injury Reports PDF Download - 4-season hourly interval collection (~50,000+ intervals, 10+ hours)"

# Resource allocation (sized for long-running downloads with comprehensive interval processing)
TASK_TIMEOUT="10h"      # Extended timeout for massive interval processing
MEMORY="2Gi"            # Memory for PDF processing and pattern analysis
CPU="1"                 # Single CPU sufficient for 4-second rate limiting

# Optional: Override defaults (these are already set in deploy script)
# REGION="us-west2"
# PROJECT_ID="nba-props-platform"  
# SERVICE_URL="https://nba-scrapers-f7p3g7f6ya-wl.a.run.app"

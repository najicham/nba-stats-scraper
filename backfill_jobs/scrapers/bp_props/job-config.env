# FILE: backfill_jobs/scrapers/bp_props/job-config.env
# 
# Configuration for BettingPros Props Backfill Job
# Used with consolidated deployment approach

# Job identification
JOB_NAME="bp-props-backfill"
JOB_SCRIPT="backfill_jobs/scrapers/bp_props/bp_props_scraper_backfill.py"
JOB_DESCRIPTION="BettingPros Historical Props Backfill - 4-season historical prop data collection"

# Resource allocation (sized for long-running historical collection)
TASK_TIMEOUT="6h"
MEMORY="2Gi"
CPU="1"

# Optional: Override defaults (these are already set in deploy script)
# REGION="us-west2"
# PROJECT_ID="nba-props-platform"  
# SERVICE_URL="https://nba-scrapers-f7p3g7f6ya-wl.a.run.app"
# FILE: backfill_jobs/scrapers/nbac_scoreboard_v2/job-config.env
# Configuration for NBA.com Scoreboard V2 Scraper Backfill Job

# Job identification
JOB_NAME="nbac-scoreboard-v2-scraper-backfill"
JOB_SCRIPT="backfill_jobs/scrapers/nbac_scoreboard_v2/nbac_scoreboard_v2_scraper_backfill.py"
JOB_DESCRIPTION="Scrape NBA.com Scoreboard V2 data with FIXED home/away assignment (2-5 min per month with 2s delays)"

# Resource allocation
TASK_TIMEOUT="1h"       # 1 hour timeout (enough for full season ~174 days)
MEMORY="2Gi"            # Light processing, mainly JSON API calls (may need proxy)
CPU="1"                 # Single CPU sufficient for sequential scraping

# Optional overrides (defaults set in deploy script)
# REGION="us-west2"
# PROJECT_ID="nba-props-platform"
# SERVICE_URL="https://nba-scrapers-f7p3g7f6ya-wl.a.run.app"
# FILE: backfill_jobs/scrapers/bdl_boxscore/job-config.env
# 
# Configuration for Ball Don't Lie Boxscore Backfill Job
# Used with consolidated deployment approach

# Job identification
JOB_NAME="bdl-boxscore-backfill"
JOB_SCRIPT="backfill_jobs/scrapers/bdl_boxscore/bdl_boxscore_scraper_backfill.py"
JOB_DESCRIPTION="Ball Don't Lie Boxscore Backfill - 4-season historical data collection"

# Resource allocation (right-sized for fast execution)
TASK_TIMEOUT="30m"
MEMORY="1Gi"
CPU="1"

# Optional: Override defaults (these are already set in deploy script)
# REGION="us-west2"
# PROJECT_ID="nba-props-platform"  
# SERVICE_URL="https://nba-scrapers-f7p3g7f6ya-wl.a.run.app"
# File: backfill_jobs/raw/nbac_team_boxscore/job-config.env
# Description: Cloud Run job configuration for NBA.com team boxscore processor backfill

# CRITICAL: Use unique name - add "processor" to distinguish from scraper jobs
JOB_NAME="nbac-team-boxscore-processor-backfill"
JOB_SCRIPT="backfill_jobs/raw/nbac_team_boxscore/nbac_team_boxscore_backfill_job.py"
JOB_DESCRIPTION="Process NBA.com team boxscore data from GCS to BigQuery with smart idempotency"

# Resources
# Medium data volume: ~2 teams per game, lightweight processing
TASK_TIMEOUT="3600"  # 1 hour
MEMORY="2Gi"
CPU="1"

# Defaults
BUCKET_NAME="nba-scraped-data"

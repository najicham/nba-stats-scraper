# File: backfill_jobs/raw/nbac_scoreboard_v2/job-config.env
# Description: Cloud Run job configuration for NBA.com Scoreboard V2 processor backfill

# CRITICAL: Use unique name - add "processor" to distinguish from scraper jobs
JOB_NAME="nbac-scoreboard-v2-processor-backfill"
JOB_SCRIPT="backfill_jobs/raw/nbac_scoreboard_v2/nbac_scoreboard_v2_raw_backfill.py"
JOB_DESCRIPTION="Process NBA.com Scoreboard V2 data from GCS to BigQuery"

# Resources
TASK_TIMEOUT="3600"  # 1 hour
MEMORY="4Gi"
CPU="2"

# Defaults (last 30 days if no args provided)
START_DATE=""  # Will default to 30 days ago in script
END_DATE=""    # Will default to today in script
BUCKET_NAME="nba-scraped-data"
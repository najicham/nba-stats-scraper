# File: backfill_jobs/raw/bettingpros_player_props/job-config.env
# Description: Cloud Run job configuration for BettingPros player props processor backfill

# CRITICAL: Use unique name - add "processor" to distinguish from scraper jobs
JOB_NAME="bettingpros-player-props-processor-backfill"
JOB_SCRIPT="backfill_jobs/raw/bettingpros_player_props/bettingpros_player_props_raw_backfill.py"
JOB_DESCRIPTION="Process BettingPros player props data from GCS to BigQuery"

# Resources (higher than usual due to complex nested JSON processing)
TASK_TIMEOUT="7200"  # 2 hours (complex transformation and large dataset)
MEMORY="8Gi"         # Higher memory for processing nested JSON structures
CPU="4"              # More CPU for flattening operations

# Default Parameters (October 2021 - Present based on your data availability)
START_DATE="2021-10-01"
END_DATE="2025-12-31"
MARKET_TYPE="points"
BUCKET_NAME="nba-scraped-data"

# Processing Configuration
DEFAULT_LIMIT=""     # No limit by default (full backfill)
DEFAULT_DRY_RUN="false"
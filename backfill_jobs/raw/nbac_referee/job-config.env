# File: backfill_jobs/raw/nbac_referee/job-config.env
# Description: Cloud Run job configuration for NBA.com referee assignments processor backfill

# CRITICAL: Use unique name - add "processor" to distinguish from scraper jobs
JOB_NAME="nbac-referee-processor-backfill"
JOB_SCRIPT="backfill_jobs/raw/nbac_referee/nbac_referee_raw_backfill.py"
JOB_DESCRIPTION="Process NBA.com referee assignments data from GCS to BigQuery"

# Resources
TASK_TIMEOUT="3600"  # 1 hour
MEMORY="4Gi"
CPU="2"

# Defaults - Following your pattern of last 30 days to today
START_DATE=""  # Will use default (30 days ago)
END_DATE=""    # Will use default (today)
BUCKET_NAME="nba-scraped-data"
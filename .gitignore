# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
eggs/
*.egg-info/
.installed.cfg
*.egg

# Virtual environment
.venv/
venv/
ENV/
env/
venv.bak/

# Root level .env file only
/.env

# Environment file patterns
.env
.env.local
.env.*
.env.*.local

# Explicitly allow these environment files
!.env.example
!**/job-config.env

# pytest cache
.pytest_cache/

# Hypothesis test database
.hypothesis/

# MyPy cache
.mypy_cache/

# VSCode settings and such (optional if you use VSCode)
.vscode/

# Mac OS / other system files
.DS_Store
Thumbs.db

# Local configuration files
*.local
*.log

# Coverage reports
htmlcov/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover

# ── Terraform working directory ──────────────────────────
.terraform/

# Local state (never commit)
terraform.tfstate*
# workspace dir
terraform.tfstate.d/
# crash diagnostics
crash.log

# ── Environment‑specific variable files ──────────────────
# dev.tfvars, staging.tfvars, prod.tfvars…
*.tfvars

# ── Editor / OS noise (optional) ─────────────────────────
*.swp
.DS_Store
Makefile.complex

# ── NBA Analytics Project Specific ──────────────────────

# Build artifacts
.last_image

# Cloud Build temporary files
/tmp/cloudbuild-*.yaml
cloudbuild-*.yaml
build-*.yaml

# Google Cloud authentication & secrets
service-account-key.json
*-service-account.json
gcp-key.json
.gcloud/
.config/gcloud/

# API keys and secrets (additional patterns)
*_key.txt
*_secret.txt
api_keys.json
secrets.json

# Application logs and debugging
logs/
*.log.*
debug.log
scraper-*.log
nba-*.log

# Local data and cache files
data/local/
cache/
tmp/
*.cache
.cache/

# Docker local artifacts (if you use Docker locally)
.dockerignore.local
docker-compose.override.yml
Dockerfile.local

# IDE files (additional)
.idea/
*.sublime-*
.atom/
.vim/
*.vim

# Backup and temporary files
*~
*.bak
*.backup
*.tmp
*.temp
*.orig

# Monitoring and profiling
profile.prof
*.prof
monitoring/local/

# Local database files (if you add local DB later)
*.db
*.sqlite
*.sqlite3

# OS artifacts (comprehensive)
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# Node.js artifacts (if you add frontend/tooling later)
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log
.npm
.yarn-integrity

# Local development overrides
local-config.yaml
dev-config.yaml
override.yaml
*-local.yaml

# Testing artifacts (additional)
.tox/
.nox/
.coverage.*
coverage.json
test-results/
test-output/

# Jupyter Notebooks (if you add data analysis)
.ipynb_checkpoints
*.ipynb

# Local scripts and notes
scratch.py
temp.py
notes.txt
TODO.local
NOTES.md

zcsv
keys/
service-account-dev.json
catboost_info/

# Model files (binary, stored in GCS)
models/
Dockerfile.backup.*
failed_games_*.json
.backups/
# Experiment model artifacts (large binaries)
ml/experiments/results/*.cbm
